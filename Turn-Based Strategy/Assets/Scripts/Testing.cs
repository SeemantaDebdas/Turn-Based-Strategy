using UnityEngine;

public class Testing : MonoBehaviour
{
    [SerializeField] MoveAction action;

    private void Start()
    {
        //systemVisual.ShowGridPositionList(action.GetValidActionGridPositionList());     
    }
}
